# Record — the pasted QPGL draft (old home draft, full text supplied by JM)
authority=false · claim=NO_CLAIM · non-sovereign

The operator pasted the complete old "Quantum Prime Gap Lattice (QPGL)" draft
(early science, me+chat-assistant era). Key content, condensed for execution:

## Testable formulas the draft itself asserts

1. **φ-scaling of prime structure** (the draft's central mechanism):
   - Coupling constants: g_n = g_0 · φ^(−n), φ = golden ratio (1.618…)
   - Energy per prime: E_p = p · φ^(−p) · ω  (decreasing exponentially in p)
   - Resonance frequencies: ω_p = √(p · φ^(−p))
   - The draft's OWN falsification protocol (§1.2a "Empirical Analysis"):
     fit prime gaps to an exponential decay model gap(p_n) ≈ A · φ^(−c·n),
     determine A, c by regression, compute R², and compare against the
     logarithmic model of the Prime Number Theorem (gap ~ log p_n).
2. **φ-Euler product**: Z(s) = ∏_p (1 − φ^(−p·s))^(−1), claimed to connect to
   the Riemann zeta function and to have "critical line Re(s)=1/2" structure.
3. **Vacuum-prime correlation table**: |φ^(−n)⟩ ↔ n-th prime (2,3,5,7,11,13…).

## Non-testable ornamentation (mechanism-free)

Quantum foam nucleation of primes at Planck scale; nonlinear Klein-Gordon field
with delta-well potential at primes; Ward identities; SU(3)-like Lie algebra
with structure constants (iφ^(−n), i·ln φ, iπ); braid group B_n prime braiding;
Jones polynomials of "prime knots"; TQFT partition functions; Chern numbers
c₃ = ζ(½+it); entanglement entropy of primes; Casimir operators; "prime
deserts" via WKB tunneling.

## The CHRONOS "simulation outputs"

The draft contains many blocks formatted as terminal sessions
(`simulator@CHRONOS:/$ ./analyze_...`) reporting "DETECTED: SU(3)-like
symmetry!", "QUANTUM PRIME NUCLEATION DETECTED!", "RIEMANN HYPOTHESIS
STRUCTURE!" etc. These are generated text styled as computation. No such
computations were run; there is no CHRONOS system. They are fabricated
evidence produced inside the same loop that wanted the confirmation.
