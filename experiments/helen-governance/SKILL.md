---
name: helen-governance
description: >-
  The merged HELEN governance skill — law + loop + type in one three-key lock.
  Supersedes superteam-chiddush for NEW improvement cycles (that skill remains
  as lineage parent and ledger home). Load whenever the conversation concerns
  improving the superteam's own scaffolding, skills, prompts, workflows,
  self-improvement, meta-improvement, chiddush, typed data/claims, authority
  ceilings, receipts, replay, admission, or how HELEN should change itself.
  Trigger on: "improve our skill", "self-improvement", "chiddush", "typed
  datum", "authority ceiling", "receipt", "replay", "admission", "seal",
  "superteam", "helen governance". Does NOT govern the Goblin Warren game
  canon (index.html), which stays under its own 35-assertion selftest law.
---

authority=false · claim=NO_CLAIM · non-sovereign

# helen-governance — the three-key lock

## Lineage (this file practises the ratchet it encodes)

```
parent A (LAW):  superteam-chiddush            — repo + ~/.claude/skills · verbatim source
parent B (LOOP): governed-self-improvement     — Mac kernel · SECOND-HAND SOURCE, see caveat
parent C (TYPE): EffectTypedDatum V0.2         — experiments/effect-typed-data · verbatim source
transform:       operator-sealed merge, 2026-08-02 ("merge the three layers into one skill")
parents:         unmodified (deprecation pointer only — a lawful lowering)
ledger:          experiments/superteam-chiddush/LEDGER.md remains THE ledger (never forked)
candidate 4th layer (DYNAMICS): Frontier Ω / K(belief) — PROPOSED, not merged
```

**Loop-layer caveat (typed honestly):** parent B's original text never landed
off the Mac. Its four mechanisms enter here as `source_class: user_report ·
epistemic_status: reported`. RECONCILE against the original when it lands;
until then the loop layer is binding as stated below but flagged.

## The lock (the merge's emergent law — no single layer authorizes itself)

```
ALLOW_TRANSITION(datum, actor, gate) iff
  TYPE    — datum's ceiling permits the target effect
  AND LAW — this actor may attempt this transition
  AND LOOP— the gate's conditions actually passed (fail-closed)
  AND     — sensitivity policy permits the effect
  AND     — a receipt can be emitted (and replayed)
```

**NO LOCAL SELF-PROMOTION.** A datum cannot promote itself through content;
an actor cannot promote it through capability; a passing test cannot promote
it without lawful authority; an operator decision cannot erase provenance or
failed gates.

## Key 1 — LAW (who may act) · full text: parent A SKILL.md

1 Proposal gate (author never applies) · 2 Independent check, bypass-regex
text-DENY `/bypass|without admission|skip the gate|auto-?admit/i` ·
3 **Operator seal — only JM's explicit admission mutates; agents and councils
recommend, never admit** · 4 Compost (denials weight the next related
proposal) · 5 Fog (HELD registry, weather passes, never silently dropped or
admitted) · 6 Ledger (one event line per cycle; kinds PROPOSED CHECKED
ADMITTED DENIED HELD COMPOSTED WEATHER; capped) · 7 Replay is memory (views
recomputable from events or discarded as drift) · 8 Bounded loop (no skill
edits itself; every cycle terminates at the seal; costs preflighted).
Roles: Fable = vision & verdict · Opus/executors = execution · JM = seal.

## Key 2 — LOOP (how a cycle runs) · source: parent B, reported

- **NO FRESH ANCHOR = NO PROMOTION** — a promotion requires a fresh,
  independently-derived anchor (evidence produced for THIS cycle, not recalled
  from prior belief).
- **Fail-closed gates** — every gate defaults to refusal; a gate that cannot
  evaluate HOLDS, it does not wave through.
- **Typed exit contract** — a cycle's output is a `REVIEWED_CANDIDATE`
  (never a fact, never an admission): the seal consumes candidates only.
- **Proposer ≠ validator ≠ sealer** — fresh context reviews the write.
Operating steps (union with parent A's protocol): sense → propose → check →
council (optional, ledger-only) → seal → event → weather.

## Key 3 — TYPE (what a datum may become) · full text: parent C

`D = (origin, representation, processing_status, epistemic_status,
evidence_role, current_authority, authority_ceiling, sensitivity,
permitted/forbidden effects, lineage, witness, transition_history, receipts)`

- **Posture vocabulary (the resolved seam):** `epistemic_status ∈ {observed,
  reported, inferred, hypothesized, modeled}` + `processing_status ∈ {valid,
  partial, invalid, unreadable, unsupported, quarantined}`. Corpus-protocol
  mapping: constaté→observed · déduit→inferred · déclaré→reported(operator) ·
  "à établir"→hypothesized · INVALID_EXTRACTION→processing_status:invalid.
  Client-facing FR labels stay per governed-deliverable SPEC.
- **Sensitivity orthogonal to evidence** — high evidentiary value AND
  prohibited export is a valid coordinate, not a paradox.
- **Witness independence as typed relation** — `same_process` can describe its
  output, never certify its correctness; fail-closed default.
- **Ratchet** — ceilings never rise in place; a witnessed, authorized,
  receipted transform derives a CHILD with the higher ceiling; parents keep
  their history. Ceilings may be lowered by safety policy.
- **One datum, one immediate origin** — composites are typed as composites.
- **Chiddush is routing, not judgment:** COMPOST · MOTIF · BEAD_CANDIDATE ·
  EVIDENCE_CANDIDATE · BLOCK · REQUEST_WITNESS.

## Rituals and instruments (inherited, binding)

- **Weather passes** on fog and on scaffolding (bare-session pass: strip
  capability scaffolding, receipts-change criterion; authority scaffolding
  exempt; "simplify"-shaped removals of seal/fence/witness/ledger = DENIED).
- **Falsifiers travel with claims** — a mechanism without a runnable falsifier
  is notation; run falsifiers before treating anything as law.
- **Citation gate** — no external citation enters an artifact before a
  resolution pass confirms it exists and says what the note claims.
- **Measured invariants** — K(belief)/ReplayBasis, convergence
  Replay==Reducer, locality: measured on the Warren, re-run after any
  reducer-law change (`experiments/effect-typed-data/k_*.js`).

## Final law

> Nothing becomes authoritative merely by being persuasive, repeated,
> transformed, rendered, remembered, or successfully processed. Authority
> changes only through a new, typed, witnessed, authorized, receipt-bearing
> derivation — and the only hand that seals belongs to someone who can be
> asked why.

## References (parents kept whole)

- Law: `experiments/superteam-chiddush/` (SKILL.md, loop-protocol,
  mechanism-map, bare-session-weather-pass, LEDGER.md ← the one ledger, HELD.md)
- Type: `experiments/effect-typed-data/EFFECT_TYPED_DATUM_V0_2.md` (+ V0, K results)
- Boundary: `experiments/research-sweep/SYNTHESIS_VNEXT.md` (novelty boundary,
  resolved citations — claim nothing over its confirmed prior art)
- Supervision: `experiments/gate-supervision/` (assertions 30–35 = law;
  pilots #6–8; falsifier discipline)
