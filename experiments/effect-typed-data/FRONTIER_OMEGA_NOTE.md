authority=false · claim=NO_CLAIM · non-sovereign · status: PROPOSED

# Frontier Ω — Constitutional Epistemic Dynamics (verifier's landing note)

Operator-supplied research program: govern knowledge, not just action.
E=(B,R,Λ,Ω); evolution E_{t+1} = Π_c ∘ Φ ∘ R(E_t), with Π_c projecting onto
the constitutional manifold M_c = {x : I_i(x)=0}. Landed as the candidate
FOURTH layer (dynamics) above law/loop/type.

## What is already mechanically real in this repo (the Noether column)

| Proposed symmetry ⇒ conserved quantity | Existing mechanical witness |
|---|---|
| Replay symmetry ⇒ deterministic replay | selftest #30 (verdict replay on frozen/cloned state) |
| Receipt symmetry ⇒ audit completeness | #32 (exact deltas, zero orphan GARDEN_EVOLVED) |
| Authority symmetry ⇒ bounded privilege | I2 "proposal ↛ state" = admitProposal sole mutator, asserted |
| Witness symmetry ⇒ non-self-certification | #34 (witness ledger-only, seed untouched) + amendment C |

The invariants I1–I3 are not aspiration: they are running assertions. The
Noether principle, restricted to this codebase, is already *observed* at n=4.

## What is notation awaiting definitions (honest fog)

Curvature K(B), action functional A, momentum P(B), energy E_c: no units, no
measure on B, no defined ReceiptCost metric yet. Until "minimal receipt cost"
is a computable function, these are organizing metaphors — valuable as
direction, inadmissible as claims. The operator's own text says this
(research program; novelty unestablished vs AGM belief revision, event
sourcing, capability systems, categorical PL). Preserved verbatim, not
upgraded.

## Cheapest next experiment

Define ReceiptCost concretely on the Warren: K(belief) = number of ledger
events whose replay is required to re-derive it. Compute K for every derived
view in the selftest walk. That makes "epistemic curvature" a measured integer
before it is a theory — the §1.2a lesson applied at birth: write the
falsifiable form first.
