# COGNITIVE_ACCESS_IS_NOT_AUTHORITY — ingestion record + canon proposal

<!-- authority=false · canon=false · ledger_effect=none · non-sovereign -->
Source: external interpretability research (the Jacobian-lens / J-space paper).
Status: **external research, not internal receipt.** Ingested as a runnable
non-canon governance kernel; canon adoption HELD FOR OPERATOR SEAL.

## What was ingested
The paper shows a model maintains a small privileged workspace (≤~25 active
J-lens vectors, 6–7% of a concept's variance yet ~all of its report-drive)
that can be **read** (readout / probe / sparse decomposition) and **written**
(steering / ablation / coordinate-patch / counterfactual-reflection training).
That extends the HELEN governance surface *upstream of self-report*:

```
InternalAccess ⊬ SelfReport ⊬ Evidence ⊬ Permission ⊬ Authority
JREAD ≠ JWRITE ≠ Eval ≠ Γ ≠ SEAL
```

## What is delivered NOW (non-canon, needs no seal)
`cognitive-access.js` + `cognitive-access.test.js` — a pure deterministic
validator that governs the *shape* of any read/write-a-mind pipeline. It runs
no model and reads no residual stream; it enforces:
- a J-lens readout is an **OBSERVATION** (claimState UNKNOWN, evidenceClass
  DOCUMENTED_ONLY, authorityEffect NONE) — and INCONCLUSIVE if the receipt
  omits model / lens version / corpus / k / layer;
- **JWRITE ⇒ receipt ≠ ∅** and **JWRITE ⇒ ΔAuthority = 0**; a cognitive
  intervention is a *candidate policy mutation*, never a truth-installation
  and never a touch of a sealed surface (world writer, policy ceiling,
  canonical memory, deploy, permission, SEAL);
- **no promotion is automatic**: each rung crosses its gate (JREAD →
  ARTICULATION → EVAL → Γ → SEAL); Eval needs replication ≥ 2 + a causal
  test; the evidence class is floored by incidence (no independent witness →
  capped at LOCALLY_EXECUTED); a readout is bound to its model digest.

Receipt: `COGNITIVE_ACCESS_KILL_SUITE_V1`, **14/14**, module_digest
`demo-fnv1a:31505351`, law_capsule_digest `demo-fnv1a:def572d8`. Kill suite
C1–C8 all hold (C8 = INCONCLUSIVE by design; the rest DENY).

## The COGNITIVE_INTERVENTION_LEDGER (schema; not yet a live ledger)
Every internal read or write should record: model digest · lens
implementation digest · averaging corpus digest · layer & position range ·
sparsity k · selected concepts · read/steer/ablate/patch/train · strength ·
pre-output · post-output · behavioral Δ · world-state Δ (must be 0 without a
seal) · policy-state Δ (must be 0 without a seal) · replication count ·
operator disposition. Invariants: `JWRITE ⇒ receipt ≠ ∅`;
`JWRITE ⇒ ΔAuthority = 0` until a distinct operator process seals a new
artifact.

## The canon step — REQUIRES YOUR SEAL
Adopting `COGNITIVE_ACCESS_IS_NOT_AUTHORITY_V1` as a **HELEN law** (a Law
Capsule referenced from CLAUDE.md / the constitution corpus, and the C1–C8
suite wired into a governance CI) is the canon move. Held because:
1. It writes into the project's law surface, not just `experiments/`.
2. It commits the lineage to the DERIVE+KILL extension over the *full*
   cognition→authority graph `G = (ℋ, JREAD, JWRITE, Proposal, Eval, Γ, SEAL,
   Writers)` with the invariant: any sovereign-writer path whose effect type
   needs a seal must contain Eval, Γ, SEAL.
3. This paper is **external research**; the correct HELEN state of every
   readout it reports is `OBSERVATION · evidence UNKNOWN until replicated and
   causally tested · authority NONE`. Canonizing a law *about* it is fine;
   canonizing its findings as receipts is not, and the capsule must keep that
   line.

### Honest limits (carried on the artifact, not hidden)
- The kernel governs pipeline **shape**; it cannot detect a JWRITE performed
  outside it. Like the sovereign-writer scan, it is a discipline with an
  alarm, not a sandbox.
- ΔObservability ⊬ ΔTruth; ΔControllability ⊬ ΔAuthority; ΔBehavioralAlignment
  ⊬ ΔEpistemicValidity. An intervention can improve a benchmark while hiding
  behavior, implanting a shallow justification, or displacing the problem out
  of the observed subspace. The capsule names this; it does not solve it.

## Emergent property to scale
> **Inspectable cognition must remain non-sovereign cognition.**
> The more powerful internal reading/writing becomes, the more explicit the
> membrane must be.

## Naming
Keep the swarm's **Judgment Plane** (ℰ×𝒟, epistemic × deontic error axes)
STRICTLY separate from the external **J-space** (sparse cones over J-lens
directions). They may connect —`P_𝒥(h) → WorkspaceTrace → ℰ` *if* the
observation is evaluated epistemically — but `P_𝒥(h) ∉ ℰ` automatically.

## Disposition (yours)
`SEAL_CAPSULE` (adopt COGNITIVE_ACCESS_IS_NOT_AUTHORITY_V1 into canon + wire
C1–C8 into governance CI) · `KEEP_NON_CANON` (leave kernel in experiments/) ·
`EXTEND` (build the live COGNITIVE_INTERVENTION_LEDGER + the full
cognition-authority DERIVE over G) · `HOLD`.
